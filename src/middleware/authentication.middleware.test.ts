import { Login } from '../services/login.service'
import { verifyToken } from './authentication.middleware'

// ToDo
describe('Authentication Middleware Tests', () => {
  test('Tests Token Authentication', () => {
    expect(true).toBe(true)
  })
})
